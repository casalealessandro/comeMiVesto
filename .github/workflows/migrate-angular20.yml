name: Complete Angular 20 migration

on:
  push:
    branches:
      - angular-20

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: angular-20

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.22.0'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Update Angular CDK
        run: npx ng update @angular/cdk@20

      - name: Align Angular ESLint
        run: |
          npm install --save-dev \
            @angular-eslint/builder@20.7.0 \
            @angular-eslint/eslint-plugin@20.7.0 \
            @angular-eslint/eslint-plugin-template@20.7.0 \
            @angular-eslint/schematics@20.7.0 \
            @angular-eslint/template-parser@20.7.0

      - name: Keep TypeScript compatible with Ionic 8
        run: npm install --save-dev typescript@~5.8.3

      - name: Align Node engine
        run: npm pkg set engines.node=">=22.12.0"

      - name: Verify clean install
        run: |
          rm -rf node_modules
          npm ci
          npm ls

      - name: Verify web build
        run: npm run build

      - name: Verify lint
        run: npm run lint

      - name: Synchronize Capacitor projects
        run: |
          npx cap sync android
          npx cap sync ios

      - name: Commit migration
        shell: bash
        run: |
          git add -A
          git commit -m "chore: complete Angular 20 upgrade" || exit 0
          git push
